

import 'dart:io';

import 'package:file_picker/file_picker.dart';

void renameFiles() async {
  // FilePickerResult? result = await FilePicker.platform.pickFiles(
  //   initialDirectory: '/assets/participant_picture/',
  //   // type: FileType.image,
  //   // allowedExtensions: ['jpg', 'jpeg'],
  // );
  // print(result?.paths);
  // print(result?.files[0].name);
  // print(result?.files);

  // final files = await Directory('/assets/participant_picture/')
  //     .listSync()
  //     .map((e) => e.uri.pathSegments.last)
  //     .toList();
  // print(files);
  // print(Directory.current);
  //
  // var dir = new Directory('/assets/participant_picture/');
  // List contents = dir.listSync();
  // for (FileSystemEntity fileOrDir in contents) {
  //   if (fileOrDir is File) {
  //     print(fileOrDir.path);
  //   } else if (fileOrDir is Directory) {
  //     print(fileOrDir.path);
  //   }
  // }
  // List allContents = dir.listSync(recursive: true);

  // List<FileSystemEntity> allContents = await Directory("../../assets/participant_picture").list().toList();
  // print(Directory("../../assets/participant_picture").path);

}